# Memoria

Hay que instalar [Typst](https://typst.app/open-source/#download) ([Guía de uso](https://typst.app/docs/reference/syntax/)).

Comandos útiles
```bash
typst compile main.py # genera main.pdf
typst watch main.py # re-compila main.pdf cada vez que se cambia algún archivo
```
